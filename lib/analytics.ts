/** Cloudflare Web Analytics. The beacon token is public by design: it ships in
 *  the page source of every visitor. Empty string means no beacon is rendered,
 *  so the site works the same before the token is filled in. */
export const CF_BEACON_TOKEN: string = "";
